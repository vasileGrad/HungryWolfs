<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/meal_tag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/round_corners_tag_view"
        android:fontFamily="@font/sf_pro_text"
        android:gravity="center"
        android:paddingStart="@dimen/margin_meal_tag_item_view_text"
        android:paddingTop="@dimen/margin_top_bottom_meal_tag_item_text"
        android:paddingEnd="@dimen/margin_meal_tag_item_view_text"
        android:paddingBottom="@dimen/margin_top_bottom_meal_tag_item_text"
        android:textColor="@color/orange"
        android:textSize="@dimen/textsize_meal_tag_item_text"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="@string/meal_tag_item_text" />

</androidx.constraintlayout.widget.ConstraintLayout>